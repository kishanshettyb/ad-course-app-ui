import { View, Text } from "react-native";
import React from "react";

const LogoTitle = (props) => {
	return (
		<View>
			<Text className="text-lg font-bold text-white">LogoTitle</Text>
		</View>
	);
};

export default LogoTitle;
